{
    "name": "cproxy",
    "main": "src/main.ts",
    "scripts": {
        "build": "bun build --compile --bytecode --minify --outfile ~/cproxy ./src/main.ts && codesign --sign - ~/cproxy"
    },
    "devDependencies": {
        "@types/bun": "^1.3.11"
    }
}